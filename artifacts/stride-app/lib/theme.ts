/**
 * Stride Global Design System
 * ─────────────────────────────────────────────────────────────────────────────
 * These are the ONLY permitted brand colors across the entire app.
 * All UI files must import from here — never hardcode hex values.
 */

/** Primary brand color: Deep Navy Blue — titles, headers, buttons */
export const NAVY = "#0A1128";

/** Secondary brand accent: Premium Gold — CTAs, focus states, highlights */
export const GOLD = "#D4AF37";

/** Background canvas: Ultra-light fresh gray — screen backgrounds */
export const BG   = "#F8F9FA";

// ── Semantic exceptions ───────────────────────────────────────────────────────
// Allowed ONLY for system alerts, error states, and status indicators.
// Do not use for decorative purposes.

/** Error / Danger — validation banners, SOS button, forbidden messages */
export const DANGER  = "#EF4444";

/** Success status — confirmed, connected, signed, active */
export const SUCCESS = "#10B981";
