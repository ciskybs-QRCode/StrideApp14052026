/**
 * Stride Feature Flags
 *
 * Feature flags control which modules are visible in the UI.
 * Flags set to `false` hide all entry points for that module
 * without removing any underlying code or data.
 *
 * MARKETPLACE: Fully built and backend-ready. Kept in "shadow mode"
 * until the next product phase (commercial launch). Flip to `true`
 * to re-enable the parent home banner, admin stats card, and all
 * marketplace screens.
 */

export const ENABLE_MARKETPLACE = false;
