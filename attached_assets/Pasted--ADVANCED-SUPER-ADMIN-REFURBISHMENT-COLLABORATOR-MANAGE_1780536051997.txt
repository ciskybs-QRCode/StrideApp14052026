🚨 ADVANCED SUPER-ADMIN REFURBISHMENT: COLLABORATOR MANAGEMENT & CUSTOM PLATFORM GATEWAYS 🚨

Role: Expert Senior Full-Stack Developer & SaaS Architect.
Objective: Upgrade the Super Admin infrastructure to support multi-collaborator access management, and implement a dynamic platform-level payment configuration engine linked to the client Admin billing view.

CRITICAL CONSTANTS & ARCHITECTURAL VISUALS:
- Master Super Admin Email (Hardcoded Fallback): "ciskybs@gmail.com"
- Theme: Strict Deep Navy Blue (#0A1128) and Gold (#D4AF37).
- Language: 100% STRICT ENGLISH across all updated and newly generated interfaces.
- Screen Containment: All new lists, management forms, and payment option rows must wrap cleanly with zero horizontal overflow on mobile viewports.
- Security Constraint: Do NOT break, disrupt, or bypass the core Supabase authentication states.

Please implement the following architectural features:

---

1. DYNAMIC SUPER-ADMIN COLLABORATOR MANAGEMENT
- Database Layer: Create/ensure a backend lookup table (e.g., `super_admin_collaborators`) to store authorized collaborator email addresses.
- Access Gate Logic: A user can access the Super Admin layout if their logged-in email MATCHES either the master hardcoded email OR exists within the live `super_admin_collaborators` table.
- UI Component (Collaborators Section): Inside the Super Admin Dashboard, add a clean, dedicated interface to manage future partners/collaborators:
  - An input field to add a new collaborator's email with an "Add Collaborator" button.
  - A clean, high-contrast vertical list displaying currently authorized collaborator emails.
  - A responsive delete icon next to each email row to immediately revoke their Super Admin access privileges from the database.

2. PLATFORM SUBSCRIPTION PAYMENT CONFIGURATION ENGINE
- UI Component (Platform Gateways Section): Inside the Super Admin Dashboard, build a comprehensive administration console to manage how clients (organization Admins) can pay for their Stride Ops subscriptions.
- Features: Provide controls to add, modify, toggle, or edit multiple payment methods, including:
  - Credit Card / Stripe
  - PayPal (with custom email/link destination fields)
  - Bank Transfer / Bonifico (with text input fields for IBAN, SWIFT, and Account Holder details)
- Behavior: These parameters must be saved directly to a global platform config table in Supabase.

3. CLIENT-SIDE DYNAMIC BILLING INTEGRATION
- File/Section: `Admin / Settings / Subscription & Billing`.
- Requirement: Completely refactor the payment method selection area. Remove any static or pre-determined payment methods.
- Logic: Wire this screen to dynamically fetch the live, active payment methods enabled by the Super Admin in Section 2.
- UI Layout: The organization Admin must see an orderly list of these configured payment options (e.g., selectable radio tiles or clean dropdown elements). Selecting an option like "Bank Transfer" must cleanly reveal the specific IBAN and wire details configured by the Super Admin, fully preparing the interface for onboarding real paying associations.
