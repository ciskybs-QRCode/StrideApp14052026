You are working on an existing production-ready app.
IMPORTANT: do NOT modify, refactor, redesign, remove, or break any existing functionality, UI, backend logic, database structure, authentication flow, or styling that is already working correctly.
Apply ONLY the following additions and improvements in a clean, professional, scalable, and backward-compatible way.

## PARENT SIDE IMPROVEMENTS

### 1. Parent Registration Expansion

Update the parent registration/onboarding flow so the parent must be able to provide all useful profile information, including:

* First name
* Last name
* Full address:

  * Street
  * House number
  * ZIP/postal code
  * City
  * State/Region
  * Country
* Phone number
* Email address
* Optional profile photo upload

Requirements:

* Keep the current registration flow intact.
* Add proper validation.
* Preserve compatibility with existing users/accounts.
* Existing users without these fields must still work correctly.
* Use responsive mobile-first UI.

---

### 2. Unique QR Code Generation for Each Parent

Create a unique QR code for every parent account.

Requirements:

* QR code must be generated automatically after registration.
* QR code must be unique and securely linked to the authenticated user profile.
* QR code must remain persistent unless regenerated by admin.
* Architecture must support future usage for:

  * fitness centers
  * gyms
  * check-in systems
  * access control
  * attendance tracking

Implementation notes:

* Store QR identifier securely in database.
* Use scalable QR generation logic.
* Add backend endpoint/service if needed.
* Do NOT break existing child QR functionality.

---

### 3. Dynamic Branding Logo on Parent Home Screen

On the parent home screen:

* Remove the current “Stride” logo shown in the top-left corner.
* Replace it dynamically with the logo uploaded by the admin inside the app branding settings/admin branding page.

Requirements:

* If admin logo exists → show admin logo.
* If no custom logo exists → gracefully fallback to current default logo.
* Do not modify the rest of the layout.

---

### 4. Display Parent and Children QR Codes

Inside the parent dashboard/home area:

Show:

* the logged-in parent QR code
* all linked children QR codes

Requirements:

* Clear and clean UI.
* Mobile optimized.
* Each QR code must have:

  * name label
  * downloadable/shareable option if already supported
* Do NOT remove existing child QR logic.

---

### 5. Document Centre Improvements

Inside Document Centre:

Users must be able to:

* click each document
* preview/open document online
* download document locally

Requirements:

* Support common file types:

  * PDF
  * images
  * DOC/DOCX if already supported
* Keep existing permissions/security rules.
* Do not change existing document architecture unless necessary.
* Use in-app viewer where possible.

---

### 6. Real Signature Support on Mobile

Documents requiring signature must support REAL touch signatures on mobile devices.

Requirements:

* User must be able to sign using finger directly on screen.
* Add a proper signature pad/canvas component.
* Add “Clear/Reset Signature” button before submission.
* Signature must save correctly to backend/database/document.
* Must work on:

  * iPhone
  * Android
  * tablets
* Ensure responsive UX and smooth touch interaction.
* Preserve existing document/signature workflows.

Implementation notes:

* Do not fake typed signatures.
* Signature image/vector must persist correctly.
* Prevent accidental submission with empty signature.

---

## GENERAL DEVELOPMENT RULES

* DO NOT change anything unrelated to the requested features.
* DO NOT redesign the app.
* DO NOT alter working APIs unless strictly necessary.
* DO NOT remove current features.
* Maintain existing coding style and architecture.
* Write production-quality code only.
* Ensure backward compatibility.
* Keep database migrations safe and non-destructive.
* Test all changes for mobile responsiveness.
* Avoid introducing regressions.
* Keep implementation fast, optimized, and professional.

At the end:

1. summarize all modified files
2. explain database changes/migrations
3. list all new dependencies added
4. provide manual testing steps
5. report any potential edge cases discovered
